<tr>
  <th>商品名</th><td><%= @item.name %></td>
</tr>
<tr>
  <th>商品説明</th><td><%= @item.introduction %></td>
</tr>
<tr>
  <th>ジャンル</th><td><%= @item.genre.name %></td>
</tr>
<tr>
  <th>税込み価格<br>(税抜価格)</th><td><%= @item.add_tax_non_taxed_price.to_s(:delimited) %>(<%= @item.non_taxed_price.to_s(:delimited) %>)円</td>
</tr>
<tr>
  <th>販売ステータス</th>
  <td>
    <% if @item.sales_status == true %>
      <%= "販売中" %>
    <% else %>
      <%= "販売停止中" %>
    <% end %>
  </td>
  <td><%= link_to "編集する", edit_admin_item_path, class: "btn btn-sm btn-success" %></td>
</tr>


<thead>
          <tr>
            <td>商品名</td>
            <td>商品説明</td>
            <td>ジャンル</td>
            <td>税込み価格<br>(税抜価格)</td>
            <td>販売ステータス</td>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= @item.name %></td>
            <td><%= @item.introduction %></td>
            <td><%= @item.genre.name %></td>
            <td><%= @item.add_tax_non_taxed_price.to_s(:delimited) %>(<%= @item.non_taxed_price.to_s(:delimited) %>)円</td>
            <td>
              <% if @item.sales_status == true %>
                <%= "販売中" %>
              <% else %>
                <%= "販売停止中" %>
              <% end %>
            </td>
            <td><%= link_to "編集する", edit_admin_item_path, class: "btn btn-sm btn-success" %></td>
          </tr>
        </tbody>